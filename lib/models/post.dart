class Post{
  int id;
  String title;
  String description;
  String imagePath;
  int likeCount;
  bool isFav;

  Post(this.id, this.title, this.description, this.imagePath, this.likeCount,
      this.isFav);

}